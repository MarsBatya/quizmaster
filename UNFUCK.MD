Main steps

1. Run Docker Desktop
2. Run WSL2
3. From windows, run `unfuck_wsl_ip.bat` to fix firewall issues due to changed wsl2 ip
4. run `. run.sh` (if it complains, run `. build.sh` before)
5. From windows, run `get_windows_ip.bat` to show a proper address to connect to
6. Try to have fun
